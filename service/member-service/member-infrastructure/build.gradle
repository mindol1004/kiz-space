bootJar.enabled = false
jar.enabled = true

dependencies {
    implementation project(':member-service:member-domain')
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    runtimeOnly 'com.h2database:h2'
}